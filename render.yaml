services:
  - type: web
    name: valodle
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.2
      - key: VALODLE_ADMIN_PASSWORD
        sync: false
